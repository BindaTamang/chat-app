let Conversation = require("../models/chat_praticipants");
const { HandleAsync } = require("../uitls/asyncHandler");

// one-to-one conversation
const OneToOneConversation = HandleAsync(async(req, res, next) => {

})



module.exports = {
    OneToOneConversation
}